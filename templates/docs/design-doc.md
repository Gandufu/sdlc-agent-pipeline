# <功能名称> 设计说明书

- 编写人：
- 编写日期：
- 对应需求规格：docs/requirements/<feature>-requirement-spec.md

## 1. 技术选型与依据

（引用插件 rules/ 中的对应约定，说明为何选用某方案）

## 2. 关键决策

逐条记录本设计的重要取舍——**每条必须列出备选方案与选择理由，不允许只写结论**。只记录影响架构/排期/可维护性的决策，琐事不入；纯复用已有框架的决定无需记录。

| # | 决策项 | 备选方案 | 选择与理由 | 影响 |
|---|---|---|---|---|
| 1 | 示例：token 存储采用 Redis | A. 本地 session；B. JWT 无状态；C. Redis 集中式 | 选 C：多实例部署需共享登录态，且框架已有 Redis 组件（见 existing-framework.md） | 新增对 Redis 可用性的依赖；退出登录即删除 token |

## 3. 模块划分

| 模块 | 职责 | 是否复用已有框架 |
|---|---|---|

## 4. 接口设计

### DES-XXX-001：<接口名称>

- 对应需求：REQ-XXX-001
- 方法/路径：`POST /api/xxx/xxx`
- 请求参数：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|

- 响应结构（遵循插件 `rules/spring.md` 统一响应体）：

| 字段 | 类型 | 说明 |
|---|---|---|

- 异常场景：

## 5. 数据模型变更

| 表名 | 变更类型(新增/修改) | 字段 | 说明 |
|---|---|---|---|

## 6. 与已有框架的接入点

（描述具体复用哪些注解/组件/表，而非新建）

## 7. 确认记录

- [ ] 用户已确认（确认人/日期：______）

---

## 附：阶段交接块（定稿必填）

> 定稿时由本阶段 agent 按 `${CLAUDE_PLUGIN_ROOT}/skills/context-handoff/SKILL.md` 在文档末尾追加机器可读的 stage-handoff 块（```yaml 围栏、首行 `# stage-handoff`，stage: design）。交付时本节须已替换为真实交接块，不得留空。
